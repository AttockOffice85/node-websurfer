// scripts/types.ts
export interface Company {
    name: string;
    city?: string;
    link?: string;
}

export interface BrowserProfile {
    name: string;
    theme: string;
    // Add other profile preferences as needed
}